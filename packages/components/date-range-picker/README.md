# Date Range Picker

Select a start and end date.

## Package

`@organization/date-range-picker`

This component is scaffolded at version `0.0.0`, which represents an unreleased
component. Complete its initialization workspace and set the version to `0.1.0`
before publishing its first release.
