# component-hub-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build component-hub-plugin` to build the library.

## Running unit tests

Run `nx test component-hub-plugin` to execute the unit tests via [Vitest](https://vitest.dev/).
